process {
   executor = 'k8s'
}

docker {
   fixOwnership = true
   runOptions = '--mount type=bind,source=/projects,target=/projects'
}

k8s {
   storageClaimName = 'nextflow-api-pvc'
   projectDir = '/workflows/projects'
}
